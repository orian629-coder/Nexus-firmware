#pragma once

// Stub for the status module (HeartbeatService). Declared to complete the spec's module tree; implemented in
// a later phase.

namespace nexus::status {
class HeartbeatService {};
}  // namespace nexus::status
