#pragma once

// Stub for the status module (AlertService). Declared to complete the spec's module tree; implemented in
// a later phase.

namespace nexus::status {
class AlertService {};
}  // namespace nexus::status
