#pragma once

// Stub: key/value settings persistence (EQ profiles, last-known state). Implemented in a later
// phase (SQLite or JSON-backed). Present now so the module tree is complete.

namespace nexus::storage {
class SettingsRepository {};
}  // namespace nexus::storage
